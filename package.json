{
  "name": "mega-ticketing",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "security": "snyk test",
    "prepare": "husky"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "turbo": "^2.0.0",
    "typescript": "^5.0.0"
  }
}
