name: Industrial Security Pipeline (Manual)
on:
  workflow_dispatch:
jobs:
  snyk-sca:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Report
        run: echo "Manual SCA audit ready."
