name: Industrial Security Pipeline (Manual)

on:
  workflow_dispatch: # Eliminamos push/pull_request para evitar fallos por tokens inexistentes en la nube

jobs:
  snyk-sca:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Report
        run: echo "Snyk SCA ready for manual execution."
