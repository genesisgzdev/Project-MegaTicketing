name: Production Release
on:
  workflow_dispatch:
    inputs:
      tag:
        description: 'Version tag'
        required: true
        default: 'v2.1.0'
jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ github.event.inputs.tag }}
          name: ${{ github.event.inputs.tag }}
          body: |
            MegaTicketing release: PostgreSQL-authoritative reservations, live seat inventory, Stripe idempotency, and Redis locking. Infrastructure deployment remains a separately gated preflight.
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
